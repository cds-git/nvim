# .NET and Angular focused nvim setup

## TODO:

* Fix neotest for dotnet, after it somehow broke
* Create a new theme or port bearded arc or HC midnight void to nvim
* Fix noice to only use the floating cmdline and search
